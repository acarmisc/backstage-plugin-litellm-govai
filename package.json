{
  "name": "@govai/backstage-plugin-litellm-monorepo",
  "private": true,
  "workspaces": {
    "packages": ["packages/*"]
  },
  "scripts": {
    "build": "yarn workspace @govai/backstage-plugin-litellm build && yarn workspace @govai/backstage-plugin-litellm-backend build",
    "lint": "yarn workspaces foreach -p run lint",
    "test": "yarn workspaces foreach -p run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}