# Dependencies
node_modules
.pnp
.pnp.js
.pnp.cjs
.yarn/

# Root has no real dependency tree of its own (see package.json) — nothing
# to lock. Each package under packages/* commits its own package-lock.json
# so `npm ci` gets reproducible, non-floating installs in CI and publish.
yarn.lock

# Build outputs (built separately in monorepo and copied)
dist/

# Testing
coverage

# Misc
.DS_Store
*.log
*.local

# IDE and editor configs
.vscode/
.idea/
*.swp
*.swo
*~

# Local cache and MCP artifacts
.claude/settings.local.json
.playwright-mcp/

# Environment and secrets
.env
.env.local

# Agent-generated and scratch files
AGENTS.md
FIXES_APPLIED.md
ISSUES_SUMMARY.md
WORK_COMPLETE.md
LITELLM_MIGRATION_HANDOFF.md
all_fixes.patch
e2e-test-config.yaml
e2e-tests.sh
fix_*.md

# Stray scratch
null
